import { defineStory } from '../../runtime'
import IconLibraryPanel from '../../runtime/components/IconLibraryPanel'

export default defineStory({
  title: '图标/图标库',
  kind: 'page',
  page: () => <IconLibraryPanel />
})
