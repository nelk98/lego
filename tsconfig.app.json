{
  "extends": "@vue/tsconfig/tsconfig.dom.json",
  "include": [
    "shared/**/*",
    "apps/**/*",
    "apps/**/*.vue",
    "packages/**/*",
    "packages/**/*.vue",
    "types/*"
  ],
  "exclude": [
    "apps/**/__tests__/*",
    "packages/**/__tests__/*",
  ],
  "compilerOptions": {
    "composite": true,
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    "baseUrl": ".",
    "allowJs": true,
    "jsx": "preserve",
    "jsxImportSource": "vue",
    "paths": {
      // 这里需要注意，每新增一个 app 都需要在这里加上一个路径别名
      // 且要在对应的 vite.config.ts 内 使用 tsconfigPaths 插件实现自动别名声明
      "@playground/*": [
        "./apps/playground/src/*"
      ]
    }
  }
}